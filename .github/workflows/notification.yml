name: Notify QA on new QA tasks

on:
  issues:
    types: bug

jobs:
  notify:
    runs-on: ubuntu-latest
    steps:
      - uses: grocery-shop/notifications@v1
      with:
        recipients: bug=@psikai
